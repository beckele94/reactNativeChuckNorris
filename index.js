/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Chuck from './src/screens/Chuck';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Chuck);
